DROP   TABLE HMS_RESULT CASCADE CONSTRAINTS;
CREATE TABLE HMS_RESULT
(
 COMPID             NUMBER(3),
 TRANSDT            DATE,
 PATIENTID          VARCHAR2(10),
 TESTID             NUMBER(8),
 RESTID             NUMBER(10),  
 RESULT             VARCHAR2(100),
 ..
 PRIMARY KEY (COMPID, TRANSDT, PATIENTID, TESTID, RESTID)
)
/
